%table.body-wrap{ bgcolor: "#f7f9fa"}
  %tbody
    %tr
      %td
      %td.container{ bgcolor: "#FFFFFF"}
        .content
          %table
            %tbody
              %tr
                %td
                  %strong= "Hi #{@invitation.email}"
                  %p= "If you don't accept the invitation, it'll be delete after #{Time.now - @invitation.updated_at} hours. Follow the link to register on RubyRoidFinance"
                  %table.btn.btn-primary{ cellpadding: "0", cellspacing: "0", border: "0"}
                    %tr
                      %td=link_to "register", new_registration_url(@invitation.invited_code)
