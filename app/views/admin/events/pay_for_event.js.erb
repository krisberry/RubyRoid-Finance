$('#flash').html("<%=j flash_message %>");
$('#<%= dom_id(@payment, :row) %>').find("td a").replaceWith("<i class='glyphicon glyphicon-ok'></i>");
$('#<%= dom_id(@payment, :row) %>').find(".amount").html("<%= number_to_currency(@payment.amount, unit: "₴",format: "%n %u") %>");